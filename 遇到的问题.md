# 遇到的问题与方案

1. 双系统硬盘空间分配。开始对已经有了的磁盘进行压缩的时候，发现可压缩空间远远小于实际空闲的空间，调试后发现是部分系统文件占用，比如分页文件。
2. vscode配置。以为settings.json 是一个本地文件后来看群里的学长发的图片才意识到。
3. json的设置格式不会修改，后来模仿着前面的格式以及百度解决了
4. git的clone。从gitee上面直接clone的文件只有master一个分支，但是为的cpp文件是写在week01分支里面的，后来加了-b的参数解决了
